import { useMemo,useState } from 'react';
import { useQuery } from '@tanstack/react-query';
import { api,ApiError } from '../api/client';
import { EmptyState,ErrorState,LoadingState } from '../components/States';
import { useI18n } from '../i18n/I18nContext';
export function Reviews(){const {t,number,dateTime}=useI18n(),[search,setSearch]=useState(''),[type,setType]=useState('all');const query=useQuery({queryKey:['external-reviews-latest'],queryFn:api.latestExternalReviews,retry:false});const records=query.data?.records||[],types=[...new Set(records.map(r=>r.source_type).filter(Boolean))] as string[],filtered=useMemo(()=>records.filter(r=>(type==='all'||r.source_type===type)&&[r.record_id,r.title,r.source_type,r.review_reason,...r.review_reasons].some(v=>v?.toLocaleLowerCase().includes(search.toLocaleLowerCase()))),[records,search,type]),retry=()=>void query.refetch();return <section className="page-section"><div className="section-heading"><div><span className="eyebrow">{t('reviews')} / 06</span><h2>{t('latestReviews')}</h2><p>{t('reviewsDescription')}</p></div><span className="count-label">{t('recordsCount',{count:number(filtered.length)})}</span></div><div className="filters"><label className="search-wrap"><span className="sr-only">{t('search')}</span><input value={search} onChange={e=>setSearch(e.target.value)} placeholder={t('safeDataSearch')}/></label><select aria-label={t('filterSourceType')} value={type} onChange={e=>setType(e.target.value)}><option value="all">{t('allTypes')}</option>{types.map(v=><option key={v}>{v}</option>)}</select></div>{query.isLoading&&<LoadingState label={t('loadingReviews')}/>} {query.isError&&(query.error instanceof ApiError&&query.error.status===404&&query.error.code==='review_not_found'?<EmptyState label={t('noReviews')}/>:query.error instanceof ApiError&&query.error.status===401?<div className="state-panel error-panel" role="alert">{t('sessionExpired')}</div>:query.error instanceof TypeError||query.error instanceof ApiError&&(query.error.status===408||query.error.status===503)?<div className="state-panel error-panel" role="alert"><strong>{t('reviewsUnavailable')}</strong><button className="button button-secondary" onClick={retry}>{t('retry')}</button></div>:<ErrorState onRetry={retry}/>)}{!query.isLoading&&!query.isError&&records.length===0&&<EmptyState label={t('noReviews')}/>} {records.length>0&&filtered.length===0&&<EmptyState label={t('noMatches')}/>} {filtered.length>0&&<div className="card-list">{filtered.map(r=><article className="review-card" key={r.record_id}><div><strong>{r.title||t('untitled')}</strong><code>{r.record_id}</code></div><dl><div><dt>{t('sourceType')}</dt><dd>{r.source_type||t('notAvailable')}</dd></div><div><dt>{t('reviewReason')}</dt><dd>{r.review_reasons.join(', ')||r.review_reason}</dd></div><div><dt>{t('classification')}</dt><dd>{r.classification_label||t('notAvailable')}</dd></div><div><dt>{t('privacy')}</dt><dd>{r.privacy_status||t('notAvailable')}</dd></div><div><dt>{t('collectedAt')}</dt><dd>{r.collected_at?dateTime(r.collected_at):t('notAvailable')}</dd></div>{r.published&&<div><dt>{t('publishedAt')}</dt><dd>{dateTime(r.published)}</dd></div>}</dl></article>)}</div>}</section>}
