import { useI18n } from '../i18n/I18nContext';
export function LoadingState({ label }: { label?: string }) { const {t}=useI18n(); return <div className="state-panel loading-panel" role="status" aria-live="polite"><span className="spinner" aria-hidden="true" /><span>{label||t('loading')}</span><span className="skeleton-lines" aria-hidden="true"><i /><i /><i /></span></div>; }
export function EmptyState({ label }: { label: string }) { return <div className="state-panel empty-panel"><span className="state-icon" aria-hidden="true">◇</span><span>{label}</span></div>; }
export function ErrorState({ onRetry }: { onRetry: () => void }) { const {t}=useI18n(); return <div className="state-panel error-panel" role="alert"><span className="state-icon" aria-hidden="true">!</span><strong>{t('loadFailed')}</strong><span>{t('connectionHint')}</span><button className="button button-secondary" onClick={onRetry}>{t('retry')}</button></div>; }
