import { useI18n } from '../i18n/I18nContext';
export function Placeholder({ title }: { title: string }) { const { t } = useI18n(); return <section className="page-section"><div className="section-heading"><div><span className="eyebrow">{t('comingSoon')}</span><h2>{title}</h2></div></div><div className="placeholder"><span className="placeholder-number">01</span><strong>{t('reservedNextPhase')}</strong><p>{t('backendContractPending')}</p></div></section>; }
