# Optional CPU inference optimization toolchain. Keep this separate from the
# training/runtime requirements until the ONNX quality and speed gates pass.
transformers==4.57.6
optimum==2.1.0
optimum-onnx[onnxruntime]==0.1.0
onnx==1.22.0
onnxruntime==1.29.0
