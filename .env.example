# Optional External Sources credentials. Leave blank unless an approved source uses them.
NVD_API_KEY=
GITHUB_TOKEN=

# Optional Tor proxy settings for a later approved dark-web phase.
TOR_PROXY_HOST=127.0.0.1
TOR_PROXY_PORT=9050
