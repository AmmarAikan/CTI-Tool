# The Contabo host has reliable IPv4 connectivity, while outbound IPv6
# connections to GitHub Container Registry reset during large image pulls.
# The CTI lab exposes and tunnels all required services over IPv4.
net.ipv6.conf.all.disable_ipv6 = 1
net.ipv6.conf.default.disable_ipv6 = 1
